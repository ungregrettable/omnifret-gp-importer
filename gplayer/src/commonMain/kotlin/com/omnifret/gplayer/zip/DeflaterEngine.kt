// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "UNNECESSARY_LATEINIT",
    "CAST_NEVER_SUCCEEDS",
    "SENSELESS_COMPARISON",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.zip
import com.omnifret.gplayer.core.*

/**
 * Low level compression engine for deflate algorithm which uses a 32K sliding window
 * with secondary compression from Huffman/Shannon-Fano codes.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
internal class DeflaterEngine
{
    private var _blockStart: Double
    
    private var _maxChain: Double = 128.0
    
    private var _niceLength: Double = 128.0
    
    private var _goodLength: Double = 8.0
    
    /**
     * Hash index of string to be inserted
     */
    private var _insertHashIndex: Double = 0.0
    
    /**
     * Points to the current character in the window.
     */
    private var _strstart: Double
    
    /**
     * This array contains the part of the uncompressed stream that
     * is of relevance.  The current character is indexed by strstart.
     */
    private var _window: com.omnifret.gplayer.core.ecmaScript.Uint8Array
    
    /**
     * Hashtable, hashing three characters to an index for window, so
     * that window[index]..window[index+2] have this hash code.
     * Note that the array should really be unsigned short, so you need
     * to and the values with 0xffff.
     */
    private var _head: com.omnifret.gplayer.core.ecmaScript.Int16Array
    
    /**
     * <code>prev[index &amp; WMASK]</code> points to the previous index that has the
     * same hash code as the string starting at index.  This way
     * entries with the same hash code are in a linked list.
     * Note that the array should really be unsigned short, so you need
     * to and the values with 0xffff.
     */
    private var _prev: com.omnifret.gplayer.core.ecmaScript.Int16Array
    
    /**
     * lookahead is the number of characters starting at strstart in
     * window that are valid.
     * So window[strstart] until window[strstart+lookahead-1] are valid
     * characters.
     */
    private var _lookahead: Double = 0.0
    
    /**
     * The input data for compression.
     */
    private var _inputBuf: com.omnifret.gplayer.core.ecmaScript.Uint8Array? = null
    
    /**
     * The offset into inputBuf, where input data starts.
     */
    private var _inputOff: Double = 0.0
    
    /**
     * The end offset of the input data.
     */
    private var _inputEnd: Double = 0.0
    
    /**
     * Set if previous match exists
     */
    private var _prevAvailable: Boolean = false
    
    private var _matchStart: Double = 0.0
    
    /**
     * Length of best match
     */
    private var _matchLen: Double = 0.0
    
    private var _pending: com.omnifret.gplayer.zip.PendingBuffer
    
    private var _huffman: com.omnifret.gplayer.zip.DeflaterHuffman
    
    public var inputCrc: com.omnifret.gplayer.zip.Crc32
    
    public constructor(pending: com.omnifret.gplayer.zip.PendingBuffer){
        this._pending = pending
        this._huffman = com.omnifret.gplayer.zip.DeflaterHuffman(pending)
        this.inputCrc = com.omnifret.gplayer.zip.Crc32()
        this._window = com.omnifret.gplayer.core.ecmaScript.Uint8Array((2.0).toDouble() * com.omnifret.gplayer.zip.DeflaterConstants.wsize)
        this._head = com.omnifret.gplayer.core.ecmaScript.Int16Array(com.omnifret.gplayer.zip.DeflaterConstants.hashSize)
        this._prev = com.omnifret.gplayer.core.ecmaScript.Int16Array(com.omnifret.gplayer.zip.DeflaterConstants.wsize)
        this._blockStart = 1.0
        this._strstart = 1.0
    }
    
    /**
     * Reset internal state
     */
    public fun reset(): Unit{
        this._huffman.reset()
        this.inputCrc.reset()
        this._blockStart = 1.0
        this._strstart = 1.0
        this._lookahead = 0.0
        this._prevAvailable = false
        this._matchLen = com.omnifret.gplayer.zip.DeflaterConstants.minMatch - (1.0).toDouble()
        if (true) {
            var i: Double = 0.0
            
            while(i < com.omnifret.gplayer.zip.DeflaterConstants.hashSize){
                this._head[(i).toInt()] = 0.0
                i++
            }
        }
        if (true) {
            var i: Double = 0.0
            
            while(i < com.omnifret.gplayer.zip.DeflaterConstants.wsize){
                this._prev[(i).toInt()] = 0.0
                i++
            }
        }
    }
    
    private fun _updateHash(): Unit{
        this._insertHashIndex = (((this._window[(this._strstart).toInt()]).toInt() shl (com.omnifret.gplayer.zip.DeflaterConstants.hashShift).toInt()) xor (this._window[(this._strstart + 1.0).toInt()]).toInt()).toDouble()
    }
    
    /**
     * Determines if more input is needed.
     */
    public fun needsInput(): Boolean{
        return this._inputEnd == this._inputOff
    }
    
    /**
     * Sets input data to be deflated.  Should only be called when <code>NeedsInput()</code>
     * returns true
     * @param buffer The buffer containing input data.
     * @param offset The offset of the first byte of data.
     * @param count The number of bytes of data to use as input.
     */
    public fun setInput(buffer: com.omnifret.gplayer.core.ecmaScript.Uint8Array, offset: Double, count: Double): Unit{
        var end: Double = offset + count
        this._inputBuf = buffer
        this._inputOff = offset
        this._inputEnd = end
    }
    
    /**
     * Deflate drives actual compression of data
     * @param flush True to flush input buffers
     * @param finish Finish deflation with the current input.
     */
    public fun deflate(flush: Boolean, finish: Boolean): Boolean{
        var progress: Boolean
        do
        {
            this.fillWindow()
            var canFlush: Boolean = flush && this._inputOff == this._inputEnd
            progress = this._deflateSlow(canFlush, finish)
        }
        while (this._pending.isFlushed && progress)
        return progress
    }
    
    /**
     */
    private fun _deflateSlow(flush: Boolean, finish: Boolean): Boolean{
        if (this._lookahead < com.omnifret.gplayer.zip.DeflaterConstants.minLookahead && !flush)
        {
            return false
        }
        while (this._lookahead >= com.omnifret.gplayer.zip.DeflaterConstants.minLookahead || flush)
        {
            if (this._lookahead == 0.0)
            {
                if (this._prevAvailable)
                {
                    this._huffman.tallyLit(((this._window[(this._strstart - (1.0).toDouble()).toInt()]).toInt() and 255).toDouble())
                }
                this._prevAvailable = false
                this._huffman.flushBlock(this._window, this._blockStart, this._strstart - this._blockStart, finish)
                this._blockStart = this._strstart
                return false
            }
            if (this._strstart >= (2.0).toDouble() * com.omnifret.gplayer.zip.DeflaterConstants.wsize - com.omnifret.gplayer.zip.DeflaterConstants.minLookahead)
            {
                this._slideWindow()
            }
            var prevMatch: Double = this._matchStart
            var prevLen: Double = this._matchLen
            if (this._lookahead >= com.omnifret.gplayer.zip.DeflaterConstants.minMatch)
            {
                var hashHead: Double = this._insertString()
                if (hashHead != 0.0 && this._strstart - hashHead <= com.omnifret.gplayer.zip.DeflaterConstants.maxDist && this._findLongestMatch(hashHead))
                {
                    if (this._matchLen == com.omnifret.gplayer.zip.DeflaterConstants.minMatch && this._strstart - this._matchStart > com.omnifret.gplayer.zip.DeflaterEngine._tooFar)
                    {
                        this._matchLen = com.omnifret.gplayer.zip.DeflaterConstants.minMatch - (1.0).toDouble()
                    }
                }
            }
            if (prevLen >= com.omnifret.gplayer.zip.DeflaterConstants.minMatch && this._matchLen <= prevLen)
            {
                this._huffman.tallyDist(this._strstart - (1.0).toDouble() - prevMatch, prevLen)
                prevLen -= 2.0
                do
                {
                    this._strstart++
                    this._lookahead--
                    if (this._lookahead >= com.omnifret.gplayer.zip.DeflaterConstants.minMatch)
                    {
                        this._insertString()
                    }
                }
                while (--prevLen > 0)
                this._strstart++
                this._lookahead--
                this._prevAvailable = false
                this._matchLen = com.omnifret.gplayer.zip.DeflaterConstants.minMatch - (1.0).toDouble()
            }
            else 
            {
                if (this._prevAvailable)
                {
                    this._huffman.tallyLit(((this._window[(this._strstart - (1.0).toDouble()).toInt()]).toInt() and 255).toDouble())
                }
                this._prevAvailable = true
                this._strstart++
                this._lookahead--
            }
            if (this._huffman.isFull())
            {
                var len: Double = this._strstart - this._blockStart
                if (this._prevAvailable)
                {
                    len--
                }
                var lastBlock: Boolean = finish && this._lookahead == 0.0 && !this._prevAvailable
                this._huffman.flushBlock(this._window, this._blockStart, len, lastBlock)
                this._blockStart += len
                return !lastBlock
            }
        }
        return true
    }
    
    /**
     * Find the best (longest) string in the window matching the
     * string starting at strstart.
     */
    private fun _findLongestMatch(curMatch: Double): Boolean{
        var paramcurMatch = curMatch
        var match: Double
        var scan: Double = this._strstart
        var scanMax: Double = scan + com.omnifret.gplayer.core.ecmaScript.Math.min(com.omnifret.gplayer.zip.DeflaterConstants.maxMatch, this._lookahead) - (1.0).toDouble()
        var limit: Double = com.omnifret.gplayer.core.ecmaScript.Math.max(scan - com.omnifret.gplayer.zip.DeflaterConstants.maxDist, 0.0)
        var window: com.omnifret.gplayer.core.ecmaScript.Uint8Array = this._window
        var prev: com.omnifret.gplayer.core.ecmaScript.Int16Array = this._prev
        var chainLength: Double = this._maxChain
        var niceLength: Double = com.omnifret.gplayer.core.ecmaScript.Math.min(this._niceLength, this._lookahead)
        this._matchLen = com.omnifret.gplayer.core.ecmaScript.Math.max(this._matchLen, com.omnifret.gplayer.zip.DeflaterConstants.minMatch - (1.0).toDouble())
        if (scan + this._matchLen > scanMax)
        {
            return false
        }
        var scanEnd1: Double = window[(scan + this._matchLen - (1.0).toDouble()).toInt()]
        var scanEnd: Double = window[(scan + this._matchLen).toInt()]
        if (this._matchLen >= this._goodLength)
        {
            chainLength = ((chainLength.toInt()) shr (2)).toDouble()
        }
        do
        {
            match = paramcurMatch
            scan = this._strstart
            if (window[(match + this._matchLen).toInt()] != scanEnd || window[(match + this._matchLen - (1.0).toDouble()).toInt()] != scanEnd1 || window[(match).toInt()] != window[(scan).toInt()] || window[(++match).toInt()] != window[(++scan).toInt()])
            {
                continue
            }
            when ((scanMax - scan) % 8.0)
            {
                1.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                2.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                3.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                4.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                5.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                6.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                7.0 -> 
                {
                    if (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
                    {
                        break
                    }
                }
                else -> { }
            }
            if (window[(scan).toInt()] == window[(match).toInt()])
            {
                do
                {
                    if (scan == scanMax)
                    {
                        ++scan
                        ++match
                        break
                    }
                }
                while (window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()] && window[(++scan).toInt()] == window[(++match).toInt()])
            }
            if (scan - this._strstart > this._matchLen)
            {
                this._matchStart = paramcurMatch
                this._matchLen = scan - this._strstart
                if (this._matchLen >= niceLength)
                {
                    break
                }
                scanEnd1 = window[(scan - (1.0).toDouble()).toInt()]
                scanEnd = window[(scan).toInt()]
            }
            paramcurMatch = ((prev[(((paramcurMatch).toInt() and (com.omnifret.gplayer.zip.DeflaterConstants.wmask).toInt()).toDouble()).toInt()]).toInt() and 65535).toDouble()
        }
        while (paramcurMatch > limit && 0.0 != --chainLength)
        return this._matchLen >= com.omnifret.gplayer.zip.DeflaterConstants.minMatch
    }
    
    /**
     * Inserts the current string in the head hash and returns the previous
     * value for this hash.
     */
    private fun _insertString(): Double{
        var hash: Double = ((((this._insertHashIndex).toInt() shl (com.omnifret.gplayer.zip.DeflaterConstants.hashShift).toInt()) xor (this._window[(this._strstart + (com.omnifret.gplayer.zip.DeflaterConstants.minMatch - (1.0).toDouble())).toInt()]).toInt()) and (com.omnifret.gplayer.zip.DeflaterConstants.hashMask).toInt()).toDouble()
        var match: Double = this._head[(hash).toInt()]
        this._prev[(((this._strstart).toInt() and (com.omnifret.gplayer.zip.DeflaterConstants.wmask).toInt()).toDouble()).toInt()] = match
        this._head[(hash).toInt()] = this._strstart
        this._insertHashIndex = hash
        return ((match).toInt() and 65535).toDouble()
    }
    
    /**
     * Fill the window
     */
    public fun fillWindow(): Unit{
        if (this._strstart >= com.omnifret.gplayer.zip.DeflaterConstants.wsize + com.omnifret.gplayer.zip.DeflaterConstants.maxDist)
        {
            this._slideWindow()
        }
        if (this._lookahead < com.omnifret.gplayer.zip.DeflaterConstants.minLookahead && this._inputOff < this._inputEnd)
        {
            var more: Double = (2.0).toDouble() * com.omnifret.gplayer.zip.DeflaterConstants.wsize - this._lookahead - this._strstart
            if (more > this._inputEnd - this._inputOff)
            {
                more = this._inputEnd - this._inputOff
            }
            this._window.set(this._inputBuf!!.subarray(this._inputOff, this._inputOff + more), this._strstart + this._lookahead)
            this.inputCrc.update(this._inputBuf!!, this._inputOff, more)
            this._inputOff += more
            this._lookahead += more
        }
        if (this._lookahead >= com.omnifret.gplayer.zip.DeflaterConstants.minMatch)
        {
            this._updateHash()
        }
    }
    
    private fun _slideWindow(): Unit{
        this._window.set(this._window.subarray(com.omnifret.gplayer.zip.DeflaterConstants.wsize, com.omnifret.gplayer.zip.DeflaterConstants.wsize + com.omnifret.gplayer.zip.DeflaterConstants.wsize), 0.0)
        this._matchStart -= com.omnifret.gplayer.zip.DeflaterConstants.wsize
        this._strstart -= com.omnifret.gplayer.zip.DeflaterConstants.wsize
        this._blockStart -= com.omnifret.gplayer.zip.DeflaterConstants.wsize
        if (true) {
            var i: Double = 0.0
            
            while(i < com.omnifret.gplayer.zip.DeflaterConstants.hashSize){
                var m: Double = ((this._head[(i).toInt()]).toInt() and 65535).toDouble()
                this._head[(i).toInt()] = if(m >= com.omnifret.gplayer.zip.DeflaterConstants.wsize)  m - com.omnifret.gplayer.zip.DeflaterConstants.wsize else 0.0
                ++i
            }
        }
        if (true) {
            var i: Double = 0.0
            
            while(i < com.omnifret.gplayer.zip.DeflaterConstants.wsize){
                var m: Double = ((this._prev[(i).toInt()]).toInt() and 65535).toDouble()
                this._prev[(i).toInt()] = if(m >= com.omnifret.gplayer.zip.DeflaterConstants.wsize)  m - com.omnifret.gplayer.zip.DeflaterConstants.wsize else 0.0
                i++
            }
        }
    }
    
    companion object{
        @kotlin.jvm.JvmStatic
        private val _tooFar: Double = 4096.0
        
    }
}

