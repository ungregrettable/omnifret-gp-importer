// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "UNNECESSARY_LATEINIT",
    "CAST_NEVER_SUCCEEDS",
    "SENSELESS_COMPARISON",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.importer
import com.omnifret.gplayer.core.*

/**
 * This public class represents the file system structure
 * stored within a GPX container file.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
internal class GpxFileSystem
{
    /**
     * You can set a file filter method using this setter. On parsing
     * the filestructure this function can determine based on the filename
     * whether this file will be available after loading.
     * This way we can reduce the amount of memory we store.
     */
    public var fileFilter: ((arg1: String) -> Boolean)
    
    /**
     * Gets the list of files stored in this FileSystem.
     */
    public var files: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.importer.GpxFile> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.importer.GpxFile>(
    )
    
    
    
    public constructor(){
        this.files = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.importer.GpxFile>(
        )
        
        this.fileFilter = fun(_: String): Boolean{
            return true
        }
        
    }
    
    /**
     * Load a complete FileSystem to the memory.
     * @param s the binary source to read from.
     */
    public fun load(s: com.omnifret.gplayer.io.IReadable): Unit{
        var src: com.omnifret.gplayer.io.BitReader = com.omnifret.gplayer.io.BitReader(s)
        this._readBlock(src)
    }
    
    /**
     * Reads the 4 byte header as a string.
     * @param src the BitInput to read from
     */
    public fun readHeader(src: com.omnifret.gplayer.io.BitReader): String{
        return this._getString(src.readBytes(4.0), 0.0, 4.0)
    }
    
    /**
     * Decompresses the given bitinput using the GPX compression format. Only use this method
     * if you are sure the binary data is compressed using the GPX format. Otherwise unexpected
     * behavior can occure.
     * @param src the bitInput to read the data from
     * @param skipHeader true if the header should NOT be included in the result byteset, otherwise false
     */
    public fun decompress(src: com.omnifret.gplayer.io.BitReader, skipHeader: Boolean = false): com.omnifret.gplayer.core.ecmaScript.Uint8Array{
        var uncompressed: com.omnifret.gplayer.io.ByteBuffer = com.omnifret.gplayer.io.ByteBuffer.empty()
        var buffer: com.omnifret.gplayer.core.ecmaScript.Uint8Array
        var expectedLength: Double = this._getInteger(src.readBytes(4.0), 0.0)
        try
        {
            while (uncompressed.length < expectedLength)
            {
                var flag: Double = src.readBits(1.0)
                if (flag == 1.0)
                {
                    var wordSize: Double = src.readBits(4.0)
                    var offset: Double = src.readBitsReversed(wordSize)
                    var size: Double = src.readBitsReversed(wordSize)
                    var sourcePosition: Double = uncompressed.length - offset
                    var toRead: Double = com.omnifret.gplayer.core.ecmaScript.Math.min(offset, size)
                    buffer = uncompressed.getBuffer()
                    uncompressed.write(buffer, sourcePosition, toRead)
                }
                else 
                {
                    var size: Double = src.readBitsReversed(2.0)
                    if (true) {
                        var i: Double = 0.0
                        
                        while(i < size){
                            uncompressed.writeByte(src.readByte())
                            i++
                        }
                    }
                }
            }
        }
        catch (e: kotlin.Throwable)
        {
            if (!(e is com.omnifret.gplayer.io.EndOfReaderError))
            {
                throw e
            }
        }
        buffer = uncompressed.getBuffer()
        var resultOffset: Double = if(skipHeader)  4.0 else 0.0
        var resultSize: Double = uncompressed.length - resultOffset
        var result: com.omnifret.gplayer.core.ecmaScript.Uint8Array = com.omnifret.gplayer.core.ecmaScript.Uint8Array(resultSize)
        var count: Double = resultSize
        result.set(buffer.subarray(resultOffset, resultOffset + count), 0.0)
        return result
    }
    
    /**
     * Reads a block from the given data source.
     * @param `data` the data source
     */
    private fun _readBlock(`data`: com.omnifret.gplayer.io.BitReader): Unit{
        var header: String = this.readHeader(`data`)
        if (header == "BCFZ")
        {
            this._readUncompressedBlock(this.decompress(`data`, true))
        }
        else if (header == "BCFS")
        {
            this._readUncompressedBlock(`data`.readAll())
        }
        else 
        {
            throw com.omnifret.gplayer.importer.UnsupportedFormatError("Unsupported format")
        }
    }
    
    /**
     * Reads an uncompressed data block into the model.
     * @param `data` the data store to read from.
     */
    private fun _readUncompressedBlock(`data`: com.omnifret.gplayer.core.ecmaScript.Uint8Array): Unit{
        var sectorSize: Double = 4096.0
        var offset: Double = sectorSize
        while (offset + 3.0 < `data`.length)
        {
            var entryType: Double = this._getInteger(`data`, offset)
            if (entryType == 2.0)
            {
                var file: com.omnifret.gplayer.importer.GpxFile = com.omnifret.gplayer.importer.GpxFile()
                file.fileName = this._getString(`data`, offset + 4.0, 127.0)
                file.fileSize = this._getInteger(`data`, offset + 140.0)
                var storeFile: Boolean = !com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(this.fileFilter)) || this.fileFilter(file.fileName)
                if (storeFile)
                {
                    this.files.push(file)
                }
                var dataPointerOffset: Double = offset + 148.0
                var sector: Double = 0.0
                var sectorCount: Double = 0.0
                var fileData: com.omnifret.gplayer.io.ByteBuffer? = if(storeFile)  com.omnifret.gplayer.io.ByteBuffer.withCapacity(file.fileSize) else null
                while (true)
                {
                    sector = this._getInteger(`data`, dataPointerOffset + (4.0).toDouble() * sectorCount++)
                    if (sector != 0.0)
                    {
                        offset = sector * sectorSize
                        if (storeFile)
                        {
                            fileData!!.write(`data`, offset, sectorSize)
                        }
                    }
                    else 
                    {
                        break
                    }
                }
                if (storeFile)
                {
                    file.`data` = com.omnifret.gplayer.core.ecmaScript.Uint8Array(com.omnifret.gplayer.core.ecmaScript.Math.min(file.fileSize, fileData!!.length))
                    var raw: com.omnifret.gplayer.core.ecmaScript.Uint8Array = fileData!!.toArray()
                    file.`data`!!.set(raw.subarray(0.0, 0.0 + file.`data`!!.length), 0.0)
                }
            }
            offset += sectorSize
        }
    }
    
    /**
     * Reads a zeroterminated ascii string from the given source
     * @param `data` the data source to read from
     * @param offset the offset to start reading from
     * @param length the max length to read
     */
    private fun _getString(`data`: com.omnifret.gplayer.core.ecmaScript.Uint8Array, offset: Double, length: Double): String{
        var buf: String = ""
        if (true) {
            var i: Double = 0.0
            
            while(i < length){
                var code: Double = ((`data`[(offset + i).toInt()]).toInt() and 255).toDouble()
                if (code == 0.0)
                {
                    break
                }
                buf += com.omnifret.gplayer.core.ecmaScript.CoreString.fromCharCode(code)
                i++
            }
        }
        return buf
    }
    
    /**
     * Reads an 4 byte signed integer from the given source
     * @param `data` the data source to read from
     * @param offset offset the offset to start reading from
     */
    private fun _getInteger(`data`: com.omnifret.gplayer.core.ecmaScript.Uint8Array, offset: Double): Double{
        return (((`data`[(offset + 3.0).toInt()]).toInt() shl 24) or ((`data`[(offset + 2.0).toInt()]).toInt() shl 16) or ((`data`[(offset + 1.0).toInt()]).toInt() shl 8) or (`data`[(offset).toInt()]).toInt()).toDouble()
    }
    
    companion object{
        @kotlin.jvm.JvmStatic
        public val HeaderBcFs: String = "BCFS"
        
        @kotlin.jvm.JvmStatic
        public val HeaderBcFz: String = "BCFZ"
        
    }
}

/**
 * this public class represents a file within the GpxFileSystem
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
internal class GpxFile
{
    public var fileName: String = ""
    
    
    public var fileSize: Double = 0.0
    
    
    public var `data`: com.omnifret.gplayer.core.ecmaScript.Uint8Array? = null
    
    
    public constructor()
}

