// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "UNNECESSARY_LATEINIT",
    "CAST_NEVER_SUCCEEDS",
    "SENSELESS_COMPARISON",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.rendering.utils
import com.omnifret.gplayer.core.*

@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class BoundsLookup
{
    public fun toJson(): com.omnifret.gplayer.collections.Map<String, Any?>{
        var json: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
        var systems: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>(
        )
        
        json.set("staffSystems", systems)
        for (system in this.staffSystems)
        {
            var g: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
            g.set("visualBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(system.visualBounds))
            g.set("realBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(system.realBounds))
            var gBars: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>(
            )
            
            g.set("bars", gBars)
            for (masterBar in system.bars)
            {
                var mb: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
                mb.set("lineAlignedBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(masterBar.lineAlignedBounds))
                mb.set("visualBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(masterBar.visualBounds))
                mb.set("realBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(masterBar.realBounds))
                mb.set("index", masterBar.index)
                mb.set("isFirstOfLine", masterBar.isFirstOfLine)
                var mbBars: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>(
                )
                
                mb.set("bars", mbBars)
                for (bar in masterBar.bars)
                {
                    var b: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
                    b.set("visualBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(bar.visualBounds))
                    b.set("realBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(bar.realBounds))
                    var bBeats: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>(
                    )
                    
                    b.set("beats", bBeats)
                    for (beat in bar.beats)
                    {
                        var bb: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
                        bb.set("visualBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(beat.visualBounds))
                        bb.set("realBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(beat.realBounds))
                        bb.set("onNotesX", beat.onNotesX)
                        bb.set("beatIndex", beat.beat.index)
                        bb.set("voiceIndex", beat.beat.voice.index)
                        bb.set("barIndex", beat.beat.voice.bar.index)
                        bb.set("staffIndex", beat.beat.voice.bar.staff.index)
                        bb.set("trackIndex", beat.beat.voice.bar.staff.track.index)
                        if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.notes)))
                        {
                            var notes: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>(
                            )
                            
                            bb.set("notes", notes)
                            for (note in beat.notes!!)
                            {
                                var n: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
                                n.set("index", note.note.index)
                                n.set("noteHeadBounds", com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsToJson(note.noteHeadBounds))
                                notes.push(n)
                            }
                        }
                        bBeats.push(bb)
                    }
                    mbBars.push(b)
                }
                gBars.push(mb)
            }
            systems.push(g)
        }
        return json
    }
    
    private var _beatLookup: com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>> = com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>>()
    
    private var _masterBarLookup: com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.rendering.utils.MasterBarBounds> = com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.rendering.utils.MasterBarBounds>()
    
    private var _currentStaffSystem: com.omnifret.gplayer.rendering.utils.StaffSystemBounds? = null
    
    /**
     * Gets a list of all individual staff systems contained in the rendered music notation.
     */
    public var staffSystems: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.StaffSystemBounds> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.StaffSystemBounds>(
    )
    
    
    
    /**
     * Gets or sets a value indicating whether this lookup was finished already.
     */
    public var isFinished: Boolean = false
    
    
    /**
     * Finishes the lookup for optimized access.
     */
    public fun finish(scale: Double = 1.0): Unit{
        for (t in this.staffSystems)
        {
            t.finish(scale)
        }
        this.isFinished = true
    }
    
    /**
     * Re-opens the lookup for registrations without discarding previously registered bounds.
     * Used by the renderer when it preserves this lookup across a partial render so that new
     * bounds for the re-layouted range can be added while preserved systems stay intact.
     */
    internal fun resetForPartialUpdate(): Unit{
        this.isFinished = false
    }
    
    /**
     * Removes all entries belonging to the given master bar index and any bars after it.
     * Used before a partial render re-registers bounds for the re-layouted range, so the
     * preserved lookup ends up with only the unchanged entries when registration begins.
     * 
     * Assumes the layout aligns its re-layouted range to system boundaries - i.e. the first
     * system to clear starts exactly at `masterBarIndex`. Caller is responsible for passing
     * the first master-bar-index of the first re-layouted system.
     */
    internal fun clearFromMasterBar(masterBarIndex: Double): Unit{
        var firstRemovedSystem: Double = -1.0
        if (true) {
            var i: Double = 0.0
            
            while(i < this.staffSystems.length){
                var systemBars: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.MasterBarBounds> = this.staffSystems[(i).toInt()].bars
                if (systemBars.length > 0 && systemBars[(0).toInt()].index >= masterBarIndex)
                {
                    firstRemovedSystem = i
                    break
                }
                i++
            }
        }
        if (firstRemovedSystem != -1.0)
        {
            this.staffSystems.splice(firstRemovedSystem, this.staffSystems.length - firstRemovedSystem)
        }
        for (key in com.omnifret.gplayer.core.ecmaScript.Array.from(this._masterBarLookup.keys()))
        {
            if (key >= masterBarIndex)
            {
                this._masterBarLookup.delete(key)
            }
        }
        for (key in com.omnifret.gplayer.core.ecmaScript.Array.from(this._beatLookup.keys()))
        {
            var list: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds> = this._beatLookup.get(key)!!
            var filtered: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds> = list.filter(fun(b: com.omnifret.gplayer.rendering.utils.BeatBounds): Boolean{
                return b.beat.voice.bar.index < masterBarIndex
            }
            )
            if (filtered.length == 0.0)
            {
                this._beatLookup.delete(key)
            }
            else if (filtered.length != list.length)
            {
                this._beatLookup.set(key, filtered)
            }
        }
        this._currentStaffSystem = null
    }
    
    /**
     * Adds a new staff sytem to the lookup.
     * @param bounds The staff system bounds to add.
     */
    public fun addStaffSystem(bounds: com.omnifret.gplayer.rendering.utils.StaffSystemBounds): Unit{
        bounds.index = this.staffSystems.length
        bounds.boundsLookup = this
        this.staffSystems.push(bounds)
        this._currentStaffSystem = bounds
    }
    
    /**
     * Adds a new master bar to the lookup.
     * @param bounds The master bar bounds to add.
     */
    public fun addMasterBar(bounds: com.omnifret.gplayer.rendering.utils.MasterBarBounds): Unit{
        if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(bounds.staffSystemBounds)))
        {
            bounds.staffSystemBounds = this._currentStaffSystem!!
            this._masterBarLookup.set(bounds.index, bounds)
            this._currentStaffSystem!!.addBar(bounds)
        }
        else 
        {
            this._masterBarLookup.set(bounds.index, bounds)
        }
    }
    
    /**
     * Adds a new beat to the lookup.
     * @param bounds The beat bounds to add.
     */
    public fun addBeat(bounds: com.omnifret.gplayer.rendering.utils.BeatBounds): Unit{
        if (!this._beatLookup.has(bounds.beat.id))
        {
            this._beatLookup.set(bounds.beat.id, com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>(
            )
            )
        }
        this._beatLookup.get(bounds.beat.id)?.push(bounds)
    }
    
    /**
     * Tries to find the master bar bounds by a given index.
     * @param index The index of the master bar to find.
     */
    public fun findMasterBarByIndex(index: Double): com.omnifret.gplayer.rendering.utils.MasterBarBounds?{
        if (this._masterBarLookup.has(index))
        {
            return this._masterBarLookup.get(index)!!
        }
        return null
    }
    
    /**
     * Tries to find the master bar bounds by a given master bar.
     * @param bar The master bar to find.
     */
    public fun findMasterBar(bar: com.omnifret.gplayer.model.MasterBar): com.omnifret.gplayer.rendering.utils.MasterBarBounds?{
        var id: Double = bar.index
        if (this._masterBarLookup.has(id))
        {
            return this._masterBarLookup.get(id)!!
        }
        return null
    }
    
    /**
     * Tries to find the bounds of a given beat.
     * @param beat The beat to find.
     */
    public fun findBeat(beat: com.omnifret.gplayer.model.Beat): com.omnifret.gplayer.rendering.utils.BeatBounds?{
        var all: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>? = this.findBeats(beat)
        return if(com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(all)))  all!![(0).toInt()] else null
    }
    
    /**
     * Tries to find the bounds of a given beat.
     * @param beat The beat to find.
     */
    public fun findBeats(beat: com.omnifret.gplayer.model.Beat): com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>?{
        var id: Double = beat.id
        if (this._beatLookup.has(id))
        {
            return this._beatLookup.get(id)!!
        }
        return null
    }
    
    /**
     * Tries to find a beat at the given absolute position.
     * @param x The absolute X-position of the beat to find.
     * @param y The absolute Y-position of the beat to find.
     */
    public fun getBeatAtPos(x: Double, y: Double): com.omnifret.gplayer.model.Beat?{
        var bottom: Double = 0.0
        var top: Double = this.staffSystems.length - (1.0).toDouble()
        var staffSystemIndex: Double = -1.0
        while (bottom <= top)
        {
            var middle: Double = ((((top + bottom) / (2.0).toDouble())).toInt() or 0).toDouble()
            var system: com.omnifret.gplayer.rendering.utils.StaffSystemBounds = this.staffSystems[(middle).toInt()]
            if (y >= system.realBounds.y && y <= system.realBounds.y + system.realBounds.h)
            {
                staffSystemIndex = middle
                break
            }
            if (y < system.realBounds.y)
            {
                top = middle - (1.0).toDouble()
            }
            else 
            {
                bottom = middle + 1.0
            }
        }
        if (staffSystemIndex == -1.0)
        {
            return null
        }
        var staffSystem: com.omnifret.gplayer.rendering.utils.StaffSystemBounds = this.staffSystems[(staffSystemIndex).toInt()]
        var bar: com.omnifret.gplayer.rendering.utils.MasterBarBounds? = staffSystem.findBarAtPos(x)
        if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(bar)))
        {
            return bar!!.findBeatAtPos(x)
        }
        return null
    }
    
    /**
     * Tries to find the note at the given position using the given beat for fast access.
     * Use  to find a beat for a given position first.
     * @param beat The beat containing the note.
     * @param x The X-position of the note.
     * @param y The Y-position of the note.
     */
    public fun getNoteAtPos(beat: com.omnifret.gplayer.model.Beat, x: Double, y: Double): com.omnifret.gplayer.model.Note?{
        var beatBounds: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.BeatBounds>? = this.findBeats(beat)
        if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beatBounds)))
        {
            for (b in beatBounds!!)
            {
                var note: com.omnifret.gplayer.model.Note? = b.findNoteAtPos(x, y)
                if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(note)))
                {
                    return note!!
                }
            }
        }
        return null
    }
    
    companion object{
        /**
         */
        @kotlin.jvm.JvmStatic
        public fun fromJson(json: com.omnifret.gplayer.collections.Map<String, Any?>?, score: com.omnifret.gplayer.model.Score): com.omnifret.gplayer.rendering.utils.BoundsLookup?{
            if (json == null)
            {
                return null
            }
            var lookup: com.omnifret.gplayer.rendering.utils.BoundsLookup = com.omnifret.gplayer.rendering.utils.BoundsLookup()
            var staffSystems: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>> = (json!!.get("staffSystems")!! as com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>)
            for (staffSystem in staffSystems)
            {
                var sg: com.omnifret.gplayer.rendering.utils.StaffSystemBounds = com.omnifret.gplayer.rendering.utils.StaffSystemBounds()
                sg.visualBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((staffSystem.get("visualBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                sg.realBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((staffSystem.get("realBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                lookup.addStaffSystem(sg)
                for (masterBar in (staffSystem.get("bars") as com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>))
                {
                    var mb: com.omnifret.gplayer.rendering.utils.MasterBarBounds = com.omnifret.gplayer.rendering.utils.MasterBarBounds()
                    mb.index = masterBar.get("index").toDouble()
                    mb.isFirstOfLine = (masterBar.get("isFirstOfLine") as Boolean)
                    mb.lineAlignedBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((masterBar.get("lineAlignedBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                    mb.visualBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((masterBar.get("visualBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                    mb.realBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((masterBar.get("realBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                    lookup.addMasterBar(mb)
                    for (bar in (masterBar.get("bars") as com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>))
                    {
                        var b: com.omnifret.gplayer.rendering.utils.BarBounds = com.omnifret.gplayer.rendering.utils.BarBounds()
                        b.visualBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((bar.get("visualBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                        b.realBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((bar.get("realBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                        mb.addBar(b)
                        for (beat in (bar.get("beats") as com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>))
                        {
                            var bb: com.omnifret.gplayer.rendering.utils.BeatBounds = com.omnifret.gplayer.rendering.utils.BeatBounds()
                            bb.visualBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((beat.get("visualBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                            bb.realBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((beat.get("realBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                            bb.onNotesX = beat.get("onNotesX").toDouble()
                            bb.beat = score.tracks[(beat.get("trackIndex").toDouble()).toInt()].staves[(beat.get("staffIndex").toDouble()).toInt()].bars[(beat.get("barIndex").toDouble()).toInt()].voices[(beat.get("voiceIndex").toDouble()).toInt()].beats[(beat.get("beatIndex").toDouble()).toInt()]
                            if (beat.has("notes"))
                            {
                                bb.notes = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.NoteBounds>(
                                )
                                
                                for (note in (beat.get("notes") as com.omnifret.gplayer.collections.List<com.omnifret.gplayer.collections.Map<String, Any?>>))
                                {
                                    var n: com.omnifret.gplayer.rendering.utils.NoteBounds = com.omnifret.gplayer.rendering.utils.NoteBounds()
                                    n.note = bb.beat.notes[(note.get("index").toDouble()).toInt()]
                                    n.noteHeadBounds = com.omnifret.gplayer.rendering.utils.BoundsLookup._boundsFromJson((note.get("noteHeadBounds") as com.omnifret.gplayer.collections.Map<String, Any?>))
                                    bb.addNote(n)
                                }
                            }
                            b.addBeat(bb)
                        }
                    }
                }
            }
            return lookup
        }
        
        /**
         */
        @kotlin.jvm.JvmStatic
        private fun _boundsFromJson(boundsRaw: com.omnifret.gplayer.collections.Map<String, Any?>): com.omnifret.gplayer.rendering.utils.Bounds{
            var b: com.omnifret.gplayer.rendering.utils.Bounds = com.omnifret.gplayer.rendering.utils.Bounds()
            b.x = boundsRaw.get("x").toDouble()
            b.y = boundsRaw.get("y").toDouble()
            b.w = boundsRaw.get("w").toDouble()
            b.h = boundsRaw.get("h").toDouble()
            return b
        }
        
        /**
         */
        @kotlin.jvm.JvmStatic
        private fun _boundsToJson(bounds: com.omnifret.gplayer.rendering.utils.Bounds): com.omnifret.gplayer.collections.Map<String, Any?>{
            var json: com.omnifret.gplayer.collections.Map<String, Any?> = com.omnifret.gplayer.collections.Map<String, Any?>()
            json.set("x", bounds.x)
            json.set("y", bounds.y)
            json.set("w", bounds.w)
            json.set("h", bounds.h)
            return json
        }
        
    }
    public constructor()
}

