// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "UNNECESSARY_LATEINIT",
    "CAST_NEVER_SUCCEEDS",
    "SENSELESS_COMPARISON",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.rendering.utils
import com.omnifret.gplayer.core.*

/**
 * Represents the bounds of a staff system.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class StaffSystemBounds
{
    /**
     * Gets or sets the index of the bounds within the parent lookup.
     * This allows fast access of the next/previous system.
     */
    public var index: Double = 0.0
    
    
    /**
     * Gets or sets the bounds covering all visually visible elements of this staff system.
     */
    public lateinit var visualBounds: com.omnifret.gplayer.rendering.utils.Bounds
    
    /**
     * Gets or sets the actual bounds of the elements in this staff system including whitespace areas.
     */
    public lateinit var realBounds: com.omnifret.gplayer.rendering.utils.Bounds
    
    /**
     * Gets or sets the list of master bar bounds related to this staff system.
     */
    public var bars: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.MasterBarBounds> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.utils.MasterBarBounds>(
    )
    
    
    
    /**
     * Gets or sets a reference to the parent bounds lookup.
     */
    public lateinit var boundsLookup: com.omnifret.gplayer.rendering.utils.BoundsLookup
    
    /**
     * Whether this system's bounds have already been scaled via `finish`. Prevents double-scaling
     * when the parent `BoundsLookup` is preserved across partial renders and `finish` is invoked
     * again on a mix of already-scaled (preserved) and newly-registered (natural-coordinate) systems.
     */
    public var isFinished: Boolean = false
    
    
    /**
     * Finished the lookup for optimized access. Idempotent: once finished, further calls are no-ops
     * so preserved systems survive partial renders without being re-scaled.
     */
    public fun finish(scale: Double = 1.0): Unit{
        if (this.isFinished)
        {
            return
        }
        this.realBounds.scaleWith(scale)
        this.visualBounds.scaleWith(scale)
        for (t in this.bars)
        {
            t.finish(scale)
        }
        this.isFinished = true
    }
    
    /**
     * Adds a new master bar to this lookup.
     * @param bounds The master bar bounds to add.
     */
    public fun addBar(bounds: com.omnifret.gplayer.rendering.utils.MasterBarBounds): Unit{
        this.boundsLookup.addMasterBar(bounds)
        bounds.staffSystemBounds = this
        this.bars.push(bounds)
    }
    
    /**
     * Tries to find the master bar bounds that are located at the given X-position.
     * @param x The X-position to find a master bar.
     */
    public fun findBarAtPos(x: Double): com.omnifret.gplayer.rendering.utils.MasterBarBounds?{
        var b: com.omnifret.gplayer.rendering.utils.MasterBarBounds? = null
        for (bar in this.bars)
        {
            if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(b)) || bar.realBounds.x < x)
            {
                b = bar
            }
            else if (x > bar.realBounds.x + bar.realBounds.w)
            {
                break
            }
        }
        return b
    }
    
    public constructor()
}

