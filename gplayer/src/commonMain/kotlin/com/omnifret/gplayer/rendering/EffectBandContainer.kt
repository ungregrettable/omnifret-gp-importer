// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.rendering
import com.omnifret.gplayer.core.*

/**
 * Wraps the whole effect band staff for having two times the same container
 * holding bands (one for the top effects, one for the bottom effects)
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
internal class EffectBandContainer
{
    private var _bands: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.EffectBand> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.EffectBand>(
    )
    
    
    private var _bandLookup: com.omnifret.gplayer.collections.Map<String, com.omnifret.gplayer.rendering.EffectBand> = com.omnifret.gplayer.collections.Map<String, com.omnifret.gplayer.rendering.EffectBand>()
    
    private var _effectBandSizingInfo: com.omnifret.gplayer.rendering.EffectBandSizingInfo? = null
    
    private var _effectInfosSortOrder: com.omnifret.gplayer.collections.ObjectDoubleMap<com.omnifret.gplayer.rendering.EffectInfo> = com.omnifret.gplayer.collections.ObjectDoubleMap<com.omnifret.gplayer.rendering.EffectInfo>()
    
    public var height: Double = 0.0
    
    
    public lateinit var infos: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.EffectBandInfo>
    
    private var _renderer: com.omnifret.gplayer.rendering.BarRendererBase
    
    private var _isTopContainer: Boolean
    
    public fun alignGlyphs(): Unit{
        for (effectBand in this._bands)
        {
            effectBand.alignGlyphs()
        }
    }
    
    public val previousContainer: com.omnifret.gplayer.rendering.EffectBandContainer?
    get(){
        return if(this._renderer.index == 0.0)  null else if(this._isTopContainer)  this._renderer.previousRenderer!!.topEffects else this._renderer.previousRenderer!!.bottomEffects
    }
    
    public val isLinkedToPreviousRenderer: Boolean
    get(){
        return this._bands.some(fun(b: com.omnifret.gplayer.rendering.EffectBand): Boolean{
            return b.isLinkedToPrevious
        }
        )
    }
    
    public constructor(renderer: com.omnifret.gplayer.rendering.BarRendererBase, isTopContainer: Boolean){
        this._renderer = renderer
        this._isTopContainer = isTopContainer
    }
    
    public fun reLayout(): Unit{
        this.resetEffectBandSizingInfo()
        this.sizeAndAlignEffectBands()
    }
    
    public fun afterStaffBarReverted(): Unit{
        this.resetEffectBandSizingInfo()
        this.sizeAndAlignEffectBands()
    }
    
    /**
     */
    public fun createVoiceGlyphs(voice: com.omnifret.gplayer.model.Voice): Unit{
        var i: Double = 0.0
        var renderer: com.omnifret.gplayer.rendering.BarRendererBase = this._renderer
        var notationSettings: com.omnifret.gplayer.NotationSettings = renderer.settings.notation
        for (info in this.infos)
        {
            if (!notationSettings.isNotationElementVisible(info.effect.notationElement))
            {
                continue
            }
            var band: com.omnifret.gplayer.rendering.EffectBand? = null
            this._effectInfosSortOrder.set(info.effect, info.order ?: i)
            for (b in voice.beats)
            {
                if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(band)) && com.omnifret.gplayer.rendering.EffectBand.shouldCreateGlyph(b, info.effect, renderer))
                {
                    band = com.omnifret.gplayer.rendering.EffectBand(voice, info.effect, this)
                    band!!.renderer = this._renderer
                    band!!.doLayout()
                    this._bands.push(band!!)
                    this._bandLookup.set("""${(voice.index).toTemplate()}.${(info.effect.effectId).toTemplate()}""", band!!)
                }
                if (band != null)
                {
                    band!!.createGlyph(b)
                }
            }
            i++
        }
    }
    
    public fun doLayout(): Unit{
        this._effectInfosSortOrder.clear()
        this._bands = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.rendering.EffectBand>(
        )
        
        this._bandLookup = com.omnifret.gplayer.collections.Map<String, com.omnifret.gplayer.rendering.EffectBand>()
        this.resetEffectBandSizingInfo()
    }
    
    public fun resetEffectBandSizingInfo(): Unit{
        if (this._renderer.index > 0)
        {
            this._effectBandSizingInfo = this.previousContainer!!._effectBandSizingInfo
        }
        else 
        {
            if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(this._effectBandSizingInfo)) && this._effectBandSizingInfo!!.owner == this))
            {
                this._effectBandSizingInfo!!.reset()
            }
            else 
            {
                this._effectBandSizingInfo = com.omnifret.gplayer.rendering.EffectBandSizingInfo(this)
            }
        }
    }
    
    public fun finalizeEffects(): Boolean{
        return this._updateEffectBandHeights(true)
    }
    
    public fun updateEffectBandHeights(): Boolean{
        return this._updateEffectBandHeights(false)
    }
    
    /**
     */
    private fun _updateEffectBandHeights(finalize: Boolean): Boolean{
        if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(this._effectBandSizingInfo)))
        {
            return false
        }
        var y: Double = 0.0
        var paddingTop: Double = 0.0
        var paddingBottom: Double = this._renderer.settings.display.effectBandPaddingBottom
        for (slot in this._effectBandSizingInfo!!.slots)
        {
            slot.shared.y = y
            for (band in slot.bands)
            {
                y += paddingTop
                band.y = y
                if (finalize)
                {
                    band.finalizeBand()
                }
                band.height = slot.shared.height
            }
            y += slot.shared.height + paddingBottom
        }
        y = com.omnifret.gplayer.core.ecmaScript.Math.ceil(y)
        if (y != this.height)
        {
            this.height = y
            return true
        }
        return false
    }
    
    /**
     */
    public fun sizeAndAlignEffectBands(register: Boolean = true): Unit{
        for (effectBand in this._bands)
        {
            effectBand.resetHeight()
            effectBand.alignGlyphs()
            if (register && !effectBand.isEmpty)
            {
                this._effectBandSizingInfo!!.register(effectBand)
            }
        }
        if (register)
        {
            this._effectBandSizingInfo!!.sortSlots(this._effectInfosSortOrder)
        }
    }
    
    /**
     */
    public fun paint(cx: Double, cy: Double, canvas: com.omnifret.gplayer.platform.ICanvas): Unit{
        var resources: com.omnifret.gplayer.RenderingResources = this._renderer.resources
        for (effectBand in this._bands)
        {
            canvas.color = if(effectBand.voice.index == 0.0)  resources.mainGlyphColor else resources.secondaryGlyphColor
            if (!effectBand.isEmpty)
            {
                effectBand.paint(cx, cy, canvas)
            }
        }
    }
    
    /**
     */
    public fun getBand(voice: com.omnifret.gplayer.model.Voice, effectId: String): com.omnifret.gplayer.rendering.EffectBand?{
        var id: String = """${(voice.index).toTemplate()}.${(effectId).toTemplate()}"""
        if (this._bandLookup.has(id))
        {
            return this._bandLookup.get(id)!!
        }
        return null
    }
    
}

