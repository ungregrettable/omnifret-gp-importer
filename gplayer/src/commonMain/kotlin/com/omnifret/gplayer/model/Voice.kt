// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.model
import com.omnifret.gplayer.core.*

/**
 * A voice represents a group of beats
 * that can be played during a bar.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class Voice
{
    private lateinit var _beatLookup: com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.model.Beat>
    
    private var _isEmpty: Boolean = true
    
    private var _isRestOnly: Boolean = true
    
    /**
     * Gets or sets the unique id of this bar.
     */
    public var id: Double = com.omnifret.gplayer.model.Voice._globalVoiceId++
    
    
    /**
     * Gets or sets the zero-based index of this voice within the bar.
     */
    public var index: Double = 0.0
    
    
    /**
     * Gets or sets the reference to the bar this voice belongs to.
     */
    public lateinit var bar: com.omnifret.gplayer.model.Bar
    
    /**
     * Gets or sets the list of beats contained in this voice.
     */
    public var beats: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.model.Beat> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.model.Beat>(
    )
    
    
    
    public val isEmpty: Boolean
    get(){
        return this._isEmpty
    }
    
    /**
     * The style customizations for this item.
     */
    public var style: com.omnifret.gplayer.model.VoiceStyle? = null
    
    internal fun forceNonEmpty(): Unit{
        this._isEmpty = false
    }
    
    public val isRestOnly: Boolean
    get(){
        return this._isRestOnly
    }
    
    /**
     * The shortest duration contained across beats in this bar.
     */
    internal var shortestDuration: com.omnifret.gplayer.model.Duration = com.omnifret.gplayer.model.Duration.DoubleWhole
    
    
    /**
     */
    public fun insertBeat(after: com.omnifret.gplayer.model.Beat, newBeat: com.omnifret.gplayer.model.Beat): Unit{
        newBeat.nextBeat = after.nextBeat
        if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(newBeat.nextBeat)))
        {
            newBeat.nextBeat!!.previousBeat = newBeat
        }
        newBeat.previousBeat = after
        newBeat.voice = this
        after.nextBeat = newBeat
        this.beats.splice(after.index + 1.0, 0.0, newBeat)
    }
    
    /**
     */
    public fun addBeat(beat: com.omnifret.gplayer.model.Beat): Unit{
        beat.voice = this
        beat.index = this.beats.length
        this.beats.push(beat)
        if (!beat.isEmpty)
        {
            this._isEmpty = false
        }
        if (!beat.isRest)
        {
            this._isRestOnly = false
        }
    }
    
    /**
     */
    private fun _chain(beat: com.omnifret.gplayer.model.Beat, sharedDataBag: com.omnifret.gplayer.collections.Map<String, Any?>? = null): Unit{
        if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(this.bar)))
        {
            return
        }
        if (beat.index < this.beats.length - (1.0).toDouble())
        {
            beat.nextBeat = this.beats[(beat.index + 1.0).toInt()]
            beat.nextBeat!!.previousBeat = beat
        }
        else if (com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.isLastOfVoice && com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.voice.bar.nextBar))))
        {
            var nextVoice: com.omnifret.gplayer.model.Voice = this.bar.nextBar!!.voices[(this.index).toInt()]
            if (nextVoice.beats.length > 0)
            {
                beat.nextBeat = nextVoice.beats[(0).toInt()]
                beat.nextBeat!!.previousBeat = beat
            }
            else 
            {
                beat.nextBeat!!.previousBeat = beat
            }
        }
        beat.chain(sharedDataBag)
    }
    
    /**
     */
    public fun addGraceBeat(beat: com.omnifret.gplayer.model.Beat): Unit{
        if (this.beats.length == 0.0)
        {
            this.addBeat(beat)
            return
        }
        var lastBeat: com.omnifret.gplayer.model.Beat = this.beats[(this.beats.length - (1.0).toDouble()).toInt()]
        this.beats.splice(this.beats.length - (1.0).toDouble(), 1.0)
        this.addBeat(beat)
        this.addBeat(lastBeat)
        this._isEmpty = false
        this._isRestOnly = false
    }
    
    /**
     */
    public fun getBeatAtPlaybackStart(playbackStart: Double): com.omnifret.gplayer.model.Beat?{
        if (this._beatLookup.has(playbackStart))
        {
            return this._beatLookup.get(playbackStart)!!
        }
        return null
    }
    
    /**
     */
    public fun finish(settings: com.omnifret.gplayer.Settings, sharedDataBag: com.omnifret.gplayer.collections.Map<String, Any?>? = null): Unit{
        this._isEmpty = true
        this._isRestOnly = true
        this._beatLookup = com.omnifret.gplayer.collections.DoubleObjectMap<com.omnifret.gplayer.model.Beat>()
        var currentGraceGroup: com.omnifret.gplayer.model.GraceGroup? = null
        if (true) {
            var index: Double = 0.0
            
            while(index < this.beats.length){
                var beat: com.omnifret.gplayer.model.Beat = this.beats[(index).toInt()]
                beat.index = index
                this._chain(beat, sharedDataBag)
                if (beat.graceType == com.omnifret.gplayer.model.GraceType.None)
                {
                    beat.graceGroup = currentGraceGroup
                    if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(currentGraceGroup)))
                    {
                        currentGraceGroup!!.isComplete = true
                    }
                    currentGraceGroup = null
                }
                else 
                {
                    if (!com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(currentGraceGroup)))
                    {
                        currentGraceGroup = com.omnifret.gplayer.model.GraceGroup()
                    }
                    currentGraceGroup!!.addBeat(beat)
                }
                if (!beat.isEmpty)
                {
                    this._isEmpty = false
                }
                if (!beat.isRest)
                {
                    this._isRestOnly = false
                }
                index++
            }
        }
        var currentDisplayTick: Double = 0.0
        var currentPlaybackTick: Double = 0.0
        this.shortestDuration = com.omnifret.gplayer.model.Duration.DoubleWhole
        if (true) {
            var i: Double = 0.0
            
            while(i < this.beats.length){
                var beat: com.omnifret.gplayer.model.Beat = this.beats[(i).toInt()]
                beat.index = i
                beat.finish(settings, sharedDataBag)
                if (beat.graceType == com.omnifret.gplayer.model.GraceType.None)
                {
                    if (beat.duration > this.shortestDuration)
                    {
                        this.shortestDuration = beat.duration
                    }
                    if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.graceGroup)))
                    {
                        var firstGraceBeat: com.omnifret.gplayer.model.Beat = beat.graceGroup!!.beats[(0).toInt()]
                        var lastGraceBeat: com.omnifret.gplayer.model.Beat = beat.graceGroup!!.beats[(beat.graceGroup!!.beats.length - (1.0).toDouble()).toInt()]
                        if (firstGraceBeat.graceType != com.omnifret.gplayer.model.GraceType.BendGrace)
                        {
                            var stolenDuration: Double = lastGraceBeat.playbackStart + lastGraceBeat.playbackDuration - firstGraceBeat.playbackStart
                            when (firstGraceBeat.graceType)
                            {
                                com.omnifret.gplayer.model.GraceType.BeforeBeat -> 
                                {
                                    if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(firstGraceBeat.previousBeat)))
                                    {
                                        firstGraceBeat.previousBeat!!.playbackDuration -= stolenDuration
                                        if (firstGraceBeat.previousBeat!!.voice == this)
                                        {
                                            currentPlaybackTick = firstGraceBeat.previousBeat!!.playbackStart + firstGraceBeat.previousBeat!!.playbackDuration
                                        }
                                        else 
                                        {
                                            currentPlaybackTick = -stolenDuration
                                        }
                                    }
                                    else 
                                    {
                                        currentPlaybackTick = -stolenDuration
                                    }
                                    for (graceBeat in beat.graceGroup!!.beats)
                                    {
                                        this._beatLookup.delete(graceBeat.playbackStart)
                                        graceBeat.playbackStart = currentPlaybackTick
                                        this._beatLookup.set(graceBeat.playbackStart, beat)
                                        currentPlaybackTick += graceBeat.playbackDuration
                                    }
                                }
                                com.omnifret.gplayer.model.GraceType.OnBeat -> 
                                {
                                    beat.playbackDuration -= stolenDuration
                                    if (lastGraceBeat.voice == this)
                                    {
                                        currentPlaybackTick = lastGraceBeat.playbackStart + lastGraceBeat.playbackDuration
                                    }
                                    else 
                                    {
                                        currentPlaybackTick = -stolenDuration
                                    }
                                }
                                else -> { }
                            }
                        }
                    }
                    beat.displayStart = currentDisplayTick
                    beat.playbackStart = currentPlaybackTick
                    this._beatLookup.set(beat.playbackStart, beat)
                }
                else 
                {
                    beat.displayStart = currentDisplayTick
                    beat.playbackStart = currentPlaybackTick
                }
                if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.fermata)))
                {
                    this.bar.masterBar.addFermata(beat.playbackStart, beat.fermata!!)
                }
                else 
                {
                    beat.fermata = this.bar.masterBar.getFermata(beat)
                }
                beat.finishTuplet()
                if (com.omnifret.gplayer.core.TypeHelper.isTruthy(com.omnifret.gplayer.core.TypeHelper.isTruthy(beat.graceGroup)))
                {
                    beat.graceGroup!!.finish()
                }
                currentDisplayTick += beat.displayDuration
                currentPlaybackTick += beat.playbackDuration
                i++
            }
        }
    }
    
    public fun calculateDuration(): Double{
        if (this.isEmpty || this.beats.length == 0.0)
        {
            return 0.0
        }
        var lastBeat: com.omnifret.gplayer.model.Beat = this.beats[(this.beats.length - (1.0).toDouble()).toInt()]
        var firstBeat: com.omnifret.gplayer.model.Beat = this.beats[(0).toInt()]
        return lastBeat.playbackStart + lastBeat.playbackDuration - firstBeat.playbackStart
    }
    
    companion object{
        @kotlin.jvm.JvmStatic
        private var _globalVoiceId: Double = 0.0
        
        @kotlin.jvm.JvmStatic
        internal fun resetIds(): Unit{
            com.omnifret.gplayer.model.Voice._globalVoiceId = 0.0
        }
        
    }
    public constructor()
}

/**
 * Lists all graphical sub elements within a  which can be styled via 
 */
public enum class VoiceSubElement(override val value: Int): com.omnifret.gplayer.core.IGPlayerEnum
{
    /**
     * All general glyphs (like notes heads and rests).
     */
    Glyphs(0);
    companion object : com.omnifret.gplayer.core.IGPlayerEnumCompanion<VoiceSubElement>{
        public override val values = arrayOf(Glyphs) 
        public override fun fromValue(v:Double): VoiceSubElement{
            return when(v.toInt()){
                Glyphs.value -> Glyphs
                else -> throw ClassCastException("No enum with value $v found")}
        }
    }
}

/**
 * Defines the custom styles for voices.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class VoiceStyle: com.omnifret.gplayer.model.ElementStyle<com.omnifret.gplayer.model.VoiceSubElement>
{
    public constructor()
    : super()
}

