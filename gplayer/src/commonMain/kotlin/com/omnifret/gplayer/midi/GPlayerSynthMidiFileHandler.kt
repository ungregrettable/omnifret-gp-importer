// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "UNNECESSARY_LATEINIT",
    "CAST_NEVER_SUCCEEDS",
    "SENSELESS_COMPARISON",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.midi
import com.omnifret.gplayer.core.*

/**
 * This implementation of the 
 * generates a  object which can be used in GPlayerSynth for playback.
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class GPlayerSynthMidiFileHandler: com.omnifret.gplayer.midi.IMidiFileHandler
{
    private var _midiFile: com.omnifret.gplayer.midi.MidiFile
    
    private var _smf1Mode: Boolean
    
    /**
     * An indicator by how many midi-ticks the song contents are shifted.
     * Grace beats at start might require a shift for the first beat to start at 0.
     * This information can be used to translate back the player time axis to the music notation.
     */
    public var tickShift: Double = 0.0
    
    
    public constructor(midiFile: com.omnifret.gplayer.midi.MidiFile, smf1Mode: Boolean = false){
        this._midiFile = midiFile
        this._smf1Mode = smf1Mode
    }
    
    /**
     * Registers a general shift of the time-axis for the generate midi file.
     */
    public override fun addTickShift(tickShift: Double): Unit{
        this._midiFile.tickShift = tickShift
        this.tickShift = tickShift
    }
    
    /**
     * Adds a time signature to the generated midi file
     */
    public override fun addTimeSignature(tick: Double, timeSignatureNumerator: Double, timeSignatureDenominator: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        var denominatorIndex: Double = 0.0
        var denominator: Double = timeSignatureDenominator
        while (true)
        {
            denominator = ((denominator).toInt() shr 1).toDouble()
            if (denominator > 0)
            {
                denominatorIndex++
            }
            else 
            {
                break
            }
        }
        this._midiFile.addEvent(com.omnifret.gplayer.midi.TimeSignatureEvent(0.0, 
        paramtick, 
        timeSignatureNumerator, 
        denominatorIndex, 
        48.0, 
        8.0))
    }
    
    /**
     * Adds a rest to the generated midi file.
     */
    public override fun addRest(track: Double, tick: Double, channel: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        if (!this._smf1Mode)
        {
            this._midiFile.addEvent(com.omnifret.gplayer.midi.NotationRestEvent(track, paramtick, channel))
        }
    }
    
    /**
     * Adds a note to the generated midi file
     */
    public override fun addNote(track: Double, 
    start: Double, 
    length: Double, 
    key: Double, 
    velocity: Double, 
    channel: Double): Unit{
        var paramstart = start
        paramstart += this.tickShift
        this._midiFile.addEvent(com.omnifret.gplayer.midi.NoteOnEvent(track, paramstart, channel, com.omnifret.gplayer.midi.GPlayerSynthMidiFileHandler._fixValue(key), com.omnifret.gplayer.midi.GPlayerSynthMidiFileHandler._fixValue(velocity)))
        this._midiFile.addEvent(com.omnifret.gplayer.midi.NoteOffEvent(track, paramstart + length, channel, com.omnifret.gplayer.midi.GPlayerSynthMidiFileHandler._fixValue(key), com.omnifret.gplayer.midi.GPlayerSynthMidiFileHandler._fixValue(velocity)))
    }
    
    /**
     * Adds a control change to the generated midi file.
     */
    public override fun addControlChange(track: Double, tick: Double, channel: Double, controller: com.omnifret.gplayer.midi.ControllerType, value: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        this._midiFile.addEvent(com.omnifret.gplayer.midi.ControlChangeEvent(track, paramtick, channel, controller, com.omnifret.gplayer.midi.GPlayerSynthMidiFileHandler._fixValue(value)))
    }
    
    /**
     * Add a program change to the generated midi file
     */
    public override fun addProgramChange(track: Double, tick: Double, channel: Double, program: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        this._midiFile.addEvent(com.omnifret.gplayer.midi.ProgramChangeEvent(track, paramtick, channel, program))
    }
    
    /**
     * Add a tempo change to the generated midi file.
     */
    public override fun addTempo(tick: Double, tempo: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        var tempoEvent: com.omnifret.gplayer.midi.TempoChangeEvent = com.omnifret.gplayer.midi.TempoChangeEvent(paramtick, 0.0)
        tempoEvent.beatsPerMinute = tempo
        this._midiFile.addEvent(tempoEvent)
    }
    
    /**
     * Add a bend to the generated midi file.
     */
    public override fun addBend(track: Double, tick: Double, channel: Double, value: Double): Unit{
        var paramtick = tick
        var paramvalue = value
        paramtick += this.tickShift
        if (paramvalue >= com.omnifret.gplayer.synth.SynthConstants.MaxPitchWheel)
        {
            paramvalue = com.omnifret.gplayer.synth.SynthConstants.MaxPitchWheel
        }
        else 
        {
            paramvalue = com.omnifret.gplayer.core.ecmaScript.Math.floor(paramvalue)
        }
        this._midiFile.addEvent(com.omnifret.gplayer.midi.PitchBendEvent(track, paramtick, channel, paramvalue))
    }
    
    /**
     * Add a bend specific to a note to the generated midi file.
     * The note does not need to be started, if this event is signaled, the next time a note
     * on this channel and key is played it will be affected. The note bend is cleared on a note-off for this key.
     */
    public override fun addNoteBend(track: Double, tick: Double, channel: Double, key: Double, value: Double): Unit{
        var paramtick = tick
        var paramvalue = value
        paramtick += this.tickShift
        if (this._smf1Mode)
        {
            this.addBend(track, paramtick, channel, paramvalue)
        }
        else 
        {
            paramvalue = (paramvalue * com.omnifret.gplayer.synth.SynthConstants.MaxPitchWheel20) / com.omnifret.gplayer.synth.SynthConstants.MaxPitchWheel
            this._midiFile.addEvent(com.omnifret.gplayer.midi.NoteBendEvent(track, paramtick, channel, key, paramvalue))
        }
    }
    
    /**
     * Indicates that the track is finished on the given ticks.
     */
    public override fun finishTrack(track: Double, tick: Double): Unit{
        var paramtick = tick
        paramtick += this.tickShift
        if (this._midiFile.format == com.omnifret.gplayer.midi.MidiFileFormat.MultiTrack || track == 0.0)
        {
            this._midiFile.addEvent(com.omnifret.gplayer.midi.EndOfTrackEvent(track, paramtick))
        }
    }
    
    companion object{
        /**
         */
        @kotlin.jvm.JvmStatic
        private fun _fixValue(value: Double): Double{
            if (value > 127)
            {
                return 127.0
            }
            if (value < 0)
            {
                return 0.0
            }
            return value
        }
        
    }
}

