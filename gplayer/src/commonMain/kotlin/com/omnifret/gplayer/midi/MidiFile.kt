// <auto-generated>
// This code was auto-generated.
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>

@file:Suppress(
    "KotlinRedundantDiagnosticSuppress",
    "RedundantVisibilityModifier",
    "RedundantExplicitType",
    "RedundantUnitReturnType",
    "RemoveRedundantQualifierName",
    "RemoveExplicitTypeArguments",
    "MemberVisibilityCanBePrivate",
    "MoveLambdaOutsideParentheses",
    "ConvertSecondaryConstructorToPrimary",
    "RemoveRedundantCallsOfConversionMethods",
    "MayBeConstant",
    "UnusedImport",
    "CanBeVal",
    "CascadeIf",
    "unused",
    "LocalVariableName",
    "NON_EXHAUSTIVE_WHEN",
    "UNCHECKED_CAST",
    "USELESS_CAST",
    "DEPRECATION",
    "PARAMETER_NAME_CHANGED_ON_OVERRIDE",
    "UNNECESSARY_NOT_NULL_ASSERTION",
    "UNNECESSARY_SAFE_CALL",
    "UNUSED_ANONYMOUS_PARAMETER",
    "UNUSED_PARAMETER",
    "UNUSED_VALUE",
    "UNREACHABLE_CODE",
    "REDUNDANT_ELSE_IN_WHEN",
    "VARIABLE_WITH_REDUNDANT_INITIALIZER"
)
package com.omnifret.gplayer.midi
import com.omnifret.gplayer.core.*

/**
 * Represents a midi file with a single track that can be played via 
 */
@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class MidiFile
{
    /**
     * Gets or sets the midi file format to use.
     */
    public var format: com.omnifret.gplayer.midi.MidiFileFormat = com.omnifret.gplayer.midi.MidiFileFormat.SingleTrackMultiChannel
    
    
    /**
     * Gets or sets the division per quarter notes.
     */
    public var division: Double = com.omnifret.gplayer.midi.MidiUtils.QuarterTime
    
    
    /**
     * An indicator by how many midi-ticks the song contents are shifted. 
     * Grace beats at start might require a shift for the first beat to start at 0.
     * This information can be used to translate back the player time axis to the music notation.
     */
    public var tickShift: Double = 0.0
    
    
    public val events: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiEvent>
    get(){
        if (this.tracks.length == 1.0)
        {
            return this.tracks[(0).toInt()].events
        }
        var events: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiEvent> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiEvent>(
        )
        
        for (t in this.tracks)
        {
            this.events.push(*t.events.spread())
        }
        events.sort(fun(a: com.omnifret.gplayer.midi.MidiEvent, b: com.omnifret.gplayer.midi.MidiEvent): Double{
            return a.tick - b.tick
        }
        )
        return events
    }
    
    /**
     * Gets a list of midi tracks.
     */
    public var tracks: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiTrack> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiTrack>(
    )
    
    
    
    /**
     */
    private fun _ensureTracks(trackCount: Double): Unit{
        while (this.tracks.length < trackCount)
        {
            this.tracks.push(com.omnifret.gplayer.midi.MidiTrack())
        }
    }
    
    /**
     * Adds the given midi event a the correct time position into the file.
     */
    public fun addEvent(e: com.omnifret.gplayer.midi.MidiEvent): Unit{
        if (this.format == com.omnifret.gplayer.midi.MidiFileFormat.SingleTrackMultiChannel)
        {
            this._ensureTracks(1.0)
            this.tracks[(0).toInt()].addEvent(e)
        }
        else 
        {
            this._ensureTracks(e.track + 1.0)
            this.tracks[(e.track).toInt()].addEvent(e)
        }
    }
    
    /**
     * Writes the midi file into a binary format.
     */
    public fun toBinary(): com.omnifret.gplayer.core.ecmaScript.Uint8Array{
        var `data`: com.omnifret.gplayer.io.ByteBuffer = com.omnifret.gplayer.io.ByteBuffer.empty()
        this.writeTo(`data`)
        return `data`.toArray()
    }
    
    /**
     * Writes the midi file as binary into the given stream.
     */
    public fun writeTo(s: com.omnifret.gplayer.io.IWriteable): Unit{
        var b: com.omnifret.gplayer.core.ecmaScript.Uint8Array = com.omnifret.gplayer.core.ecmaScript.Uint8Array(com.omnifret.gplayer.collections.List(77.0, 84.0, 104.0, 100.0))
        s.write(b, 0.0, b.length)
        com.omnifret.gplayer.io.IOHelper.writeInt32BE(s, 6.0)
        com.omnifret.gplayer.io.IOHelper.writeInt16BE(s, this.format.toDouble())
        com.omnifret.gplayer.io.IOHelper.writeInt16BE(s, this.tracks.length)
        com.omnifret.gplayer.io.IOHelper.writeInt16BE(s, this.division)
        for (track in this.tracks)
        {
            track.writeTo(s)
        }
    }
    
    companion object{
        /**
         */
        @kotlin.jvm.JvmStatic
        public fun writeVariableInt(s: com.omnifret.gplayer.io.IWriteable, value: Double): Unit{
            var paramvalue = value
            var array: com.omnifret.gplayer.core.ecmaScript.Uint8Array = com.omnifret.gplayer.core.ecmaScript.Uint8Array(4.0)
            var n: Double = 0.0
            do
            {
                array[(n++).toInt()] = ((paramvalue).toInt() and 127).toDouble()
                paramvalue = ((paramvalue.toInt()) shr (7)).toDouble()
            }
            while (paramvalue > 0)
            while (n > 0)
            {
                n--
                if (n > 0)
                {
                    s.writeByte(((array[(n).toInt()]).toInt() or 128).toDouble())
                }
                else 
                {
                    s.writeByte(array[(n).toInt()])
                }
            }
        }
        
    }
    public constructor()
}

/**
 * Lists the different midi file formats which are supported for export.
 */
public enum class MidiFileFormat(override val value: Int): com.omnifret.gplayer.core.IGPlayerEnum
{
    /**
     * A single track multi channel file (SMF Type 0)
     */
    SingleTrackMultiChannel(0),
    /**
     * A multi track file (SMF Type 1)
     */
    MultiTrack(1);
    companion object : com.omnifret.gplayer.core.IGPlayerEnumCompanion<MidiFileFormat>{
        public override val values = arrayOf(SingleTrackMultiChannel, MultiTrack) 
        public override fun fromValue(v:Double): MidiFileFormat{
            return when(v.toInt()){
                SingleTrackMultiChannel.value -> SingleTrackMultiChannel
                MultiTrack.value -> MultiTrack
                else -> throw ClassCastException("No enum with value $v found")}
        }
    }
}

@kotlin.contracts.ExperimentalContracts
@kotlin.ExperimentalUnsignedTypes
public class MidiTrack
{
    /**
     * Gets a list of midi events sorted by time.
     */
    public var events: com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiEvent> = com.omnifret.gplayer.collections.List<com.omnifret.gplayer.midi.MidiEvent>(
    )
    
    
    
    /**
     * Adds the given midi event a the correct time position into the file.
     */
    public fun addEvent(e: com.omnifret.gplayer.midi.MidiEvent): Unit{
        if (this.events.length == 0.0 || e.tick >= this.events[(this.events.length - (1.0).toDouble()).toInt()].tick)
        {
            this.events.push(e)
        }
        else 
        {
            var insertPos: Double = this.events.length
            while (insertPos > 0)
            {
                var prevItem: com.omnifret.gplayer.midi.MidiEvent = this.events[(insertPos - (1.0).toDouble()).toInt()]
                if (prevItem.tick > e.tick)
                {
                    insertPos--
                }
                else 
                {
                    break
                }
            }
            this.events.splice(insertPos, 0.0, e)
        }
    }
    
    /**
     * Writes the midi track as binary into the given stream.
     */
    public fun writeTo(s: com.omnifret.gplayer.io.IWriteable): Unit{
        var trackData: com.omnifret.gplayer.io.ByteBuffer = com.omnifret.gplayer.io.ByteBuffer.empty()
        var previousTick: Double = 0.0
        for (midiEvent in this.events)
        {
            var delta: Double = midiEvent.tick - previousTick
            com.omnifret.gplayer.midi.MidiFile.writeVariableInt(trackData, delta)
            midiEvent.writeTo(trackData)
            previousTick = midiEvent.tick
        }
        var b: com.omnifret.gplayer.core.ecmaScript.Uint8Array = com.omnifret.gplayer.core.ecmaScript.Uint8Array(com.omnifret.gplayer.collections.List(77.0, 84.0, 114.0, 107.0))
        s.write(((b as com.omnifret.gplayer.core.ecmaScript.Uint8Array)), 0.0, b.length)
        var `data`: com.omnifret.gplayer.core.ecmaScript.Uint8Array = trackData.toArray()
        com.omnifret.gplayer.io.IOHelper.writeInt32BE(s, `data`.length)
        s.write(`data`, 0.0, `data`.length)
    }
    
    public constructor()
}

